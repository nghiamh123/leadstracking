; Cố định cho mọi máy (CI, VPS dùng mặc định false) - ghi đè legacy-peer-deps trong ~/.npmrc
; để lockfile luôn giống nhau, tránh npm ci trên CI báo lệch và npm install trên VPS ghi lại lockfile.
legacy-peer-deps=false
